server.port=8081
spring.application.name=solar-terrain-backend

# Firebase Configuration
firebase.config.path=firebase-service-account.json

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:19006
